// 在 OpenSCAD 中打开本文件后按 F5 预览（或菜单 Design → Preview）。
// 若视窗空白：View → Zoom to fit / Home。
//
// 显示模式：table_module.scad 中 leftFrameDisplayMode / rightFrameDisplayMode
// 桌面左右宽 table_width = 2000 mm；左右框 + 三根拉结横管见 table_frames_both_preview()
include <table_module.scad>

table_frames_both_preview();
